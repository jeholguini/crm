// Configuración para la extensión ia-search
// Este archivo contiene configuración sensible y NO debe subirse a git

module.exports = function (env) {
  return {
    OPENAI_API_KEY: env.OPENAI_API_KEY
  }
};
